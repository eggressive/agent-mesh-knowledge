# Agent-Mesh Living Blackboard — Python Dependencies
# Install: pip install -r requirements.txt
#
# Minimal core. Archived dependencies (slack, chromadb, cryptography, PyNaCl)
# are no longer required — legacy OpenClaw/Matrix infrastructure is decommissioned.

# Core
PyYAML>=6.0

# Development/Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
