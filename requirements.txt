# Multi-Agent Knowledge Mesh - Python Dependencies
# Install: pip install -r requirements.txt

# Core dependencies (all scripts)
PyYAML>=6.0

# Authentication (sign/verify messages)
cryptography>=41.0.0
PyNaCl>=1.5.0

# Slack Fallback Bot
slack-sdk>=3.26.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# Memory vector (Neuromancer's Python implementation)
# Note: Clawdy uses Node.js version in tools/memory-vector/
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Development/Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
